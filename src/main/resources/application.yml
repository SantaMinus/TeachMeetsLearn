spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/learn
    driverClassName: org.postgresql.Driver
    username: postgres
    password: postgres
security:
  jwt:
    token:
      secret-key: ${JWT_SECRET}